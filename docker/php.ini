[PHP]
default_charset = "UTF-8"
memory_limit = 756M
upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 300

; Disable opcache so file changes (e.g. in the mounted module) are visible immediately
[opcache]
opcache.enable = 0
